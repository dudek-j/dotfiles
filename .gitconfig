[user]
	email = dudek@student.chalmers.se\n
	name = Jakub Dudek
[core]
	editor = code --wait
	trustctime = true
	excludesfile = /Users/duddan/.gitignore
[diff]
	tool = code
[difftool]
	prompt = false
[difftool "code"]
	cmd = code --wait --diff $LOCAL $REMOTE

[mergetool "code"]
	cmd = code --wait --diff $LOCAL $REMOTE

